import React, { Component } from 'react';import ReactDOM from 'react-dom'import { Router, Route, browserHistory } from 'react-router';// function Main() {// 	return(// 		<div>// 			{this.props.children}// 		</div>// 	)// }class Main extends Component {	constructor(props){		super(props);	}	render() {		return(			<div>				{this.props.children}			</div>		)	}}function Mine() {	return (		<div>			Mine		</div>	)}function Home() {	return(		<div onClick={() => {browserHistory.push('mine')}}>home</div>	)}function Service() {	return (		<div onClick={() => {browserHistory.push('service')}}> service</div>	)}const routes = {	path: '/',	component: Main,	indexRoute: {		component: Home	},	childRoutes: [		{			path: 'home',			component: Home		},		{			path: 'mine',			component: Mine		},		{			path: 'service',			component: Service		}	]};const AppRouter = (	<Router routes={routes} history={browserHistory}/>);try {	ReactDOM.render( AppRouter, document.getElementById('app'))} catch (err) {	console.log ('err:', err)}